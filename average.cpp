#include <iostream>
using namespace std;
int main()
{
  float a, b;
  cout << "Enter the number : ";
  cin >> a;
  cout << "Enter the number : ";
  cin >> b;
  float avg;
  avg = (a + b) / 2;
  cout << "Average of two number : " << avg;
  return 0;
}